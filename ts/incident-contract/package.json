{
  "name": "@auto-pigeon/incident-contract",
  "version": "1.0.0",
  "description": "The cross-stack incident contract: a versioned, bounded incident envelope, the canonical incident-code taxonomy, the correlation-id convention every Auto-Pigeon component propagates, and the central redaction rules. Contract data plus a dependency-free reference implementation — no transport, no SDK, no running service.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.mjs",
  "types": "./src/index.d.ts",
  "files": [
    "schema",
    "src",
    "README.md"
  ],
  "exports": {
    ".": {
      "types": "./src/index.d.ts",
      "default": "./src/index.mjs"
    },
    "./schema/envelope": "./schema/incident-envelope-1.0.schema.json",
    "./schema/codes": "./schema/incident-codes.json",
    "./schema/redaction": "./schema/redaction-rules.json",
    "./package.json": "./package.json"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "ajv": "^8.20.0"
  }
}
